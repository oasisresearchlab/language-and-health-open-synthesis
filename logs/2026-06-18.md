review-app sprint with claude — went from prototype to a deployed, end-to-end clinician review tool. see [[review-app]] + [[deploying-to-vercel]].

## ~12:30 — review app deployed + LOS/readmission batch curated

shipped the whole thing and verified it live on vercel.

**deploy (functional on vercel):**
- review routes were reading `../graph` + `../data` at request time (not on serverless) → made everything self-contained in `site/`: review loaders build from the bundled `graph-data.generated.json` (`loadGeneratedGraph`), precomputed anchors/pages/regions moved to committed `site/review-data/`.
- PDFs serve from a **private Supabase Storage bucket** via short-lived signed URLs (`/api/pdf` + service-role key), streamed same-origin. batch-only uploads via `scripts/upload-review-pdfs.mjs`. copyright stays protected; gate-able later.
- env: `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY` + `SUPABASE_SERVICE_ROLE_KEY` in vercel. interim access = vercel shareable link.

**the 4 steps:**
1. shareable link — done (Joel).
2. **cleaned 4 duplicate template-stub EVDs** (Allan + Karliner), repointed/deduped every claim/source/QUE reference to the filled counterparts, re-exported (IDs renumbered), audits clean, no findings lost. kept 3 non-dups (Abedini ×2, Adams) as extraction TODOs.
3. **aligned the completeness pass** with the accuracy pass — pdf.js pane (in-doc search + scroll + highlight), locate buttons, viewport-locked layout. shared helpers → `lib/review-search.ts`.
4. **curated the LOS + readmission clusters** → `ACCURACY_BATCH` = 5 papers (Allan, Karliner, [[@Lindholm_2012_Professional_language]], Wallbrecht, Greenky), `CURATED_EVD_TITLES` allowlist shows exactly **8 EVDs** (4 LOS + 4 readmission, spanning surgical/inpatient/ED/peds and effect/null). all verified rubric-complete (verbatim-OK, What/How/Who each grounded in a quote, polarity wired, exact quote regions).

**also along the way:** maintainer queue (`/review/queue` — triage, disagreement detection, CSV/JSON export); fast Base-UI tooltips; the "missing" verdict (5th, → re-extract) + guide updated; dropped the EVD "Tagged" bullet (tagging belongs to a later synthesis step); fixed a pdf.js worker/API version mismatch.

**verified live:** 5 papers render with the right curated counts, Lindholm PDF streams from the private bucket, quote anchors highlight exactly, judgments write to the central store + show in the queue.

ready to hand the shareable link to reviewers (Rachel, Gezzer, Ibne, Miles).

**open threads (parked):** whole-figure bbox highlight (issue #6); a real per-user access gate; extract the 3 kept stubs; precision/agreement analysis off the queue export.

claude did the build; commits on `review-app-prototype`.
