---
NodeFormality: draft
NodeType: Evidence
Source:
EvidenceType:
tags:
languageConcordanceFactor:
healthOutcome:
deliveryContext:
---
## Description

> [!info] State the finding in 2–4 plain sentences, with the number(s) and a (Fig. N) / (Table N) reference. **Embed the grounding figure/table FIRST** (it becomes the node's key image), then the verbatim quote(s) + page number(s).
>
> ![[<citekey>-fig<N>.png]]   <!-- or table_<n>; from data/figures[_pdf]/<citekey>/ -->
>
> "<verbatim finding quote>" (<Author>, <year>, p. <N>)

## Methods Context

### What?

> [!info] The **observable** itself — the outcome/measure that was recorded (NOT the design). e.g. "30-day readmission rate", "diagnostic sensitivity", "interpreter-session setup time".
>
> "<verbatim quote naming the measure/outcome>" (<Author>, <year>, p. <N>)

### How?

> [!info] The **design + procedure** used to observe it (study design, comparison, analysis). Link to an [[ART - ...]] when a specific system/intervention is involved.
>
> "<verbatim quote describing the design/procedure>" (<Author>, <year>, p. <N>)

### Who?

> [!info] The **equivalence class** this generalizes to — setting, sample, participants, model system, hospital setting; sample-size flow (N → exclusions → N_final) where applicable.
>
> "<verbatim quote describing the sample/setting>" (<Author>, <year>, p. <N>)

## Other Notes

<!-- Edges are authored elsewhere: CLMs/EPs list this EVD under their Supporting/Contradicting Evidence (EVD→CLM/EP supports/opposes); CVTs list it under Qualifies (CVT→EVD qualifies). -->
